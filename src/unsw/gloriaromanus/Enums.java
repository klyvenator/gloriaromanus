package unsw.gloriaromanus;

public class Enums {
    public enum Range {
        MELEE,
        RANGED
    }

    public enum Condition {
        TREASURY,
        WEALTH,
        CONQUER
    }

    public enum Operation {
        AND,
        OR
    }
}

